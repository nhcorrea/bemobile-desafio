import { ContainerTitle, TextTitle } from "./styles";

export default function TitleHeader () {
	return (
		<ContainerTitle>
			<TextTitle>
                    Funcionários
			</TextTitle>
		</ContainerTitle>
	);
}